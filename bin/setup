#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

# Gem Update System
gem update --system

# Install Bundle 
gem install bundle

# Bundle Installs Required Gems
bundle install

# Cleaning up Gem Updates
gem cleanup
